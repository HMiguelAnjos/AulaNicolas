let string = 'Henrique'
const number = 30
var float = 2.3
let boolean = true

const test = string + ' Nicolas'

console.log(test)

//String - são textos e precisam ter aspas '' para declarar a variavel
//Number - são numeros inteiros e não se usa aspas para serem declarados
//Float - são numeros fracionados/não inteiros e não se usa aspas para serem declarados
//Boolean - são apenas apenas dois valores, true(verdadeiro) ou false(falso)

/*
-Quando declarado com const a variável não pode mudar seu valor
-Quando declarado com let a variável pode ser alterada
-Quando declarado com var a variável pode ser alterada
*/